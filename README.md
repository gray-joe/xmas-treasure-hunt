# Xmas Treasure Hunt App

A simple, static content puzzle app for an xmas treasure hunt. Each puzzle requires the
previous one to be solved in order to unlock the next.

## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```
